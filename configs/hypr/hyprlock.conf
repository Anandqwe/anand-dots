# ╔══════════════════════════════════════════════╗
# ║         anand-dots — Hyprlock Config          ║
# ║  Lock screen with blurred wallpaper backdrop  ║
# ╚══════════════════════════════════════════════╝

source = ~/.config/hypr/theme.conf

general {
    ignore_empty_input = true
}

# ── Background ──────────────────────────────────
background {
    monitor =
    path = screenshot
    blur_passes = 3
    blur_size = 6
    contrast = 0.9
    brightness = 0.7
    vibrancy = 0.17
}

# ── Password Input ──────────────────────────────
input-field {
    monitor =
    size = 250, 50
    dots_size = 0.33
    dots_spacing = 0.15
    dots_center = true
    dots_rounding = -1
    inner_color = rgba(10131ccc)
    font_color = rgb(e5e6f4)
    font_family = JetBrainsMono Nerd Font
    outer_color = rgb(b2c5ff)
    outline_thickness = 2
    fade_on_empty = true
    fade_timeout = 1000
    placeholder_text = <i>Password...</i>
    hide_input = false
    rounding = 10
    check_color = rgb(b0b5df)
    fail_color = rgb(ffb4ab)
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    capslock_color = rgb(acb7d9)
    position = 0, -20
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

# ── Clock ───────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] echo "$(date +%H:%M)"
    color = rgb(e5e6f4)
    font_size = 80
    font_family = JetBrainsMono Nerd Font
    position = 0, 200
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

# ── Date ────────────────────────────────────────
label {
    monitor =
    text = cmd[update:60000] echo "$(date '+%A, %B %d')"
    color = rgb(c4c6d3)
    font_size = 20
    font_family = JetBrainsMono Nerd Font
    position = 0, 120
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

# ── Username ────────────────────────────────────
label {
    monitor =
    text = $USER
    color = rgb(b2c5ff)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = 0, 50
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}
